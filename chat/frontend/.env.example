# URL du backend App Runner (laisser vide en dev local → proxy Vite vers localhost:8002)
VITE_BACKEND_URL=https://<app-runner-url>

# SHA-256 du mot de passe investisseur (générer avec : echo -n "monmotdepasse" | shasum -a 256)
VITE_ACCESS_TOKEN_HASH=<sha256_hex>
